Higher-order
def sum(a: Int, b:Int ) = a + b


Immutable values, no side effects,
function(anonymous,first-class, higher-order),recursion
referential transparency,substitution model,currification,lazy and eager evaluation,tail recursion,composition



currification
